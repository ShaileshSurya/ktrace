# Application Configuration
# Place this file in your classpath (src/main/resources)

# ktrace: Enable or disable MDC logging (trace IDs in application logs)
# Default: false (disabled)
# Note: Trace headers are ALWAYS injected into Kafka messages regardless of this setting
ktrace.mdc.enabled=false
